{
	"name": "arrowfinds",
	"version": "1.0.0",
	"description": "dropshipping products finder",
	"author": "Yonatan Kreiner",
	"private": true,
	"scripts": {
		"express": "npm install express",
		"path": "npm install path",
		"compression": "npm install compression",
		"helmet": "npm install helmet",
		"postinstall": "npm-run-all express path compression helmet"
	}
 }
